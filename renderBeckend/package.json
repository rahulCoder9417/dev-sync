{
  "name": "devsync-terminal",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "prisma generate && tsx watch server.ts",

    "build": "tsc",

    "prebuild": "if [ \"$SKIP_PRISMA_GENERATE\" != \"1\" ]; then prisma generate; fi",
    "start": "npm run generate:runtime && node dist/server.js",
    "generate:runtime": "prisma generate",

    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@clerk/backend": "^1.5.0",
    "@prisma/client": "^7.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "prisma": "^7.0.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
